<Window x:Class="NugetHelper.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:NugetHelper"
		d:DataContext="{d:DesignInstance Type=local:MainWindowViewModel}"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
	<Window.Resources>
		<Style TargetType="Button">
			<Setter Property="Margin" Value="5"/>
		</Style>
		<Style TargetType="TextBlock">
			<Setter Property="Margin" Value="5"/>
		</Style>
		<Style TargetType="TextBox">
			<Setter Property="Margin" Value="5"/>
		</Style>
		<Style TargetType="RadioButton">
			<Setter Property="Margin" Value="5"/>
		</Style>
		<Style TargetType="Label">
			<Setter Property="Margin" Value="5"/>
		</Style>
		<Style TargetType="ComboBox">
			<Setter Property="Margin" Value="5"/>
		</Style>
	</Window.Resources>
	<Grid Margin="10">
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="*"/>
		</Grid.RowDefinitions>
		<StackPanel Orientation="Horizontal" Grid.Row="0" Margin="5">
			<TextBlock Text="当前版本" />
			<TextBlock Text="{Binding CurrentVersion}" />
			<TextBlock Text="目标版本" />
			<TextBox Text="{Binding TargetVersion}"  Width="200" />
		</StackPanel>
		<StackPanel Orientation="Horizontal" Grid.Row="1" Margin="5">
			<Button Content="Detect" Command="{Binding DetectCommand}" Width="120" />
			<TextBlock Text="Running Sln : "  Margin="5"/>
			<ComboBox ItemsSource="{Binding AvailableSLNs}" SelectedItem="{Binding SelectedSLN}"/>
		</StackPanel>
		<StackPanel Orientation="Horizontal" Grid.Row="2" Margin="5">
			<TextBlock Text="Props path" VerticalAlignment="Center"/>
			<TextBox Text="{Binding SelectedSLNPath}" Width="250" IsReadOnly="True"/>
		</StackPanel>
		<StackPanel Orientation="Horizontal" Grid.Row="3" HorizontalAlignment="Center" VerticalAlignment="Top" Margin="10">
			<Button Content="Apply" Command="{Binding ApplyCommand}" Width="100"/>
			<Button Content="Restore" Command="{Binding RestoreCommand}" Width="100" />
		</StackPanel>
	</Grid>
</Window>

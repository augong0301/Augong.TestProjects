<Window x:Class="OpenCVTest.WPF.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:OpenCVTest.WPF"
		d:DataContext="{d:DesignInstance Type=local:MainViewModel}"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
	<Window.Resources>
		<Style TargetType="Button">
			<Setter Property="Margin" Value="10"/>
		</Style>
	</Window.Resources>
	<Grid>
		<Grid.ColumnDefinitions>
			<ColumnDefinition Width="*"/>
			<ColumnDefinition Width="*"/>
			<ColumnDefinition Width="auto" MinWidth="100"/>
		</Grid.ColumnDefinitions>
		<Grid.RowDefinitions>
			<RowDefinition Height="*"/>
			<RowDefinition Height="auto"/>
		</Grid.RowDefinitions>
		<Image Source="{Binding OriginImage}" Margin="10"/>
		<Image Source="{Binding AfterImage}" Margin="10" Grid.Column="1"/>
		<ScrollViewer Grid.Column="2" Margin="10">
			<TextBlock Text="{Binding Comparisons}"  />
		</ScrollViewer>

		<UniformGrid Grid.Row="1" Grid.ColumnSpan="4" Columns="8">
			<TextBox Text="{Binding FileName,UpdateSourceTrigger=PropertyChanged}" Height="20"/>
			<Button Content="Canny + hoff" Command="{Binding EdgeDectionCommand}"/>
			<TextBlock Text="Range" Height="20" Margin="5" HorizontalAlignment="Right" VerticalAlignment="Center"/>
			<TextBox Text="{Binding Range,UpdateSourceTrigger=PropertyChanged}" Height="20"/>
			<TextBlock Text="Step" Height="20" Margin="5" HorizontalAlignment="Right" VerticalAlignment="Center"/>
			<TextBox Text="{Binding Step,UpdateSourceTrigger=PropertyChanged}" Height="20"/>
			<Button Content="SteppedBinary" Command="{Binding SteppedBinaryCommand}"/>
			<Button Content="Watershed" Command="{Binding WaterShedCommand}"/>
		</UniformGrid>


	</Grid>
</Window>

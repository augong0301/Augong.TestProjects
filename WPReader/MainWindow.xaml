<Window x:Class="WPReader.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WPReader"
		d:DataContext="{d:DesignInstance Type=local:MainViewModel}"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
	<Window.Resources>
		<Style TargetType="TextBlock">
			<Setter Property="Margin" Value="5"/>
			<Setter Property="Height" Value="20"/>
		</Style>
		<Style TargetType="TextBox">
			<Setter Property="Margin" Value="5"/>
			<Setter Property="Height" Value="20"/>
		</Style>
		<Style TargetType="Button">
			<Setter Property="Margin" Value="5"/>
			<Setter Property="Height" Value="20"/>
		</Style>
	</Window.Resources>
	<Grid>
		<Grid.ColumnDefinitions>
			<ColumnDefinition Width="*"/>
		</Grid.ColumnDefinitions>
		<Grid.RowDefinitions>
			<RowDefinition Height="*"/>
			<RowDefinition Height="*"/>
		</Grid.RowDefinitions>
		<UniformGrid Columns="3" VerticalAlignment="Top">
			<TextBlock Text="原始WP文件路径"/>
			<TextBox Text="{Binding WPFilePath, UpdateSourceTrigger=PropertyChanged}"/>
			<Button Content="选择原始WP文件" Command="{Binding SelectWPCommand}"/>
			<TextBlock Text="输出EXCEL路径"/>
			<TextBox Text="{Binding OutputFolder, UpdateSourceTrigger=PropertyChanged}"/>
			<Button Content="选择输出EXCEL路径" Command="{Binding SelectOutputFolderCommand}"/>
			<Button Content="导出EXCEL" Command="{Binding ExportExcelCommand}"/>
		</UniformGrid>
		<TextBlock Text="{Binding Log}"/>
	</Grid>
</Window>
